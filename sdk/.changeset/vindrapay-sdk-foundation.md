---
"vindrapay-sdk": minor
---

Initial SDK foundation: `HttpRequester` transport with timeout/retry/auth, typed error hierarchy mapped from backend envelopes, async pagination over `{ items, total }` lists, and frozen camelCase entity types.
